# This file is no longer imported.
# MCP server configs have moved to config/mcp_servers/*.exs
# Each server gets its own file for easier hot-reload.
# See npx.exs and gdrive.exs for examples.
#
# Hot-reload at runtime (no VM restart):
#   Eai.MCP.reload!()